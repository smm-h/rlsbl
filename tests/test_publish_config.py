"""Tests for publish config -- target-level publish() methods and config helpers were removed in Phase 9.

Publishing is now handled by pipeline classes in rlsbl/pipelines/ and tested
in test_pipelines.py and test_pipeline_types.py.
"""
