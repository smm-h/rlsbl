"""Tests for scope-aware release flow (subdir targets).

The generic scoped-release infrastructure remains in the release command for
any future subdir-scoped targets, but codehome is now root-scoped (plugin repo
model) and no longer uses --scope. These tests are kept as a placeholder; the
codehome-specific scoped tests have been removed.
"""
