app = "rlsbl"

# Purity standard for the `pure` flag below.
#
# pure   = the check starts ONLY allowlisted read-only programs. The allowlist
#          is rlsbl/observe_allowlist.py, and its written standard is "no
#          user-visible mutation": ref updates, index writes and credential
#          emission are refused there, so anything that reaches the list
#          changes nothing a user would notice. A check that spawns no program
#          at all is trivially pure.
# impure  = the check starts a program that is NOT on that list. Every impure
#          check today runs a tool that writes -- ruff rewrites files, `uv
#          sync` materializes an environment, the test suites and gradle build.
#          "Sometimes shells out to a writing tool" means impure.
#
# This replaced "the check spawns no subprocess at all". That older rule forced
# nine checks that spawn only read-only local git to be declared impure, and it
# quietly misdeclared two that DO spawn: the retired `local-tag` ran
# `git tag --list`, and `config-schema` can reach `go list` on its error path.
# Under the rule above both are legitimately pure, and `config-schema` is
# declared so deliberately rather than by accident. The full record is in
# rlsbl/observe_allowlist.py.
#
# Verifiable rather than asserted: tests/test_check_purity.py executes every
# pure-declared check under an effects observer and fails on any spawn whose
# argv matches no allowlist prefix.
#
# `needs_network` is orthogonal and unchanged: it says whether the check needs
# the network to answer at all, never whether it may mutate.

[checks.lock]
tags = ["project"]
severity = "warn"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.version-consistency]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.name-consistency]
tags = ["project"]
severity = "warn"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.license-consistency]
tags = ["project"]
severity = "warn"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.description-consistency]
tags = ["project"]
severity = "warn"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.private-hook-stale]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.config-schema]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.license-file]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.unpublished-refs]
tags = ["release"]
severity = "error"
fast = false
pure = false
needs_network = true
depends_on = ["version-consistency"]

[checks.branch-sync]
tags = ["release"]
severity = "error"
fast = true
pure = false
needs_network = true
depends_on = []

[checks.changelog-entry]
tags = ["changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["version-consistency"]

[checks.library-lint]
tags = ["quality", "preflight"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = []
scope = "workspace:library"

# ---------------------------------------------------------------------------
# Changelog validation checks (from changelog/validate.py)
# ---------------------------------------------------------------------------

[checks.changelog-hashes]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.changelog-range]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["changelog-hashes"]

[checks.changelog-coverage]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["changelog-hashes"]

[checks.changelog-orphans]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.changelog-schema]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.changelog-user-facing]
tags = ["changelog", "preflight-changelog"]
severity = "warn"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.changelog-batch-commits]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.changelog-batch-entries]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

# A pure nudge (not in preflight-changelog): the absent-key warning is visible
# pressure under `rlsbl check`, never a release blocker. The actual enforcement
# is the error-severity changelog-format-version-gate, which IS in preflight.
[checks.changelog-format-version]
tags = ["changelog"]
severity = "warn"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.changelog-format-version-gate]
tags = ["changelog", "preflight-changelog"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

# ---------------------------------------------------------------------------
# Workspace checks (from doctor.py ad-hoc + monorepo lint)
# ---------------------------------------------------------------------------

# router-filters-fresh: the generated ci-router.yml's paths filters are derived
# from the workspace (each member's territory, the territories of everything it
# depends on transitively in every scope, the workspace-root manifests and
# lockfiles, the tool's own machinery, and -- for the root member -- negated
# excludes of every other territory). A committed block that no longer matches
# that derivation under-triggers or over-triggers member CI, and an
# under-triggered member's job concludes `skipped` on the very commit a release
# tags, which the publish gate refuses. Regenerates in memory and compares, so
# it is pure (workspace.toml + manifests + the router YAML; no subprocess or
# network). In "preflight" so a stale router blocks the release it would break.
# Loads the workspace itself rather than trusting ctx.projects: the releasable
# preflight context carries one member, and a partial member list derives the
# wrong excludes.
[checks.router-filters-fresh]
tags = ["workspace", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.workspace-ci-router]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.workspace-ci-synced]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["workspace-ci-router"]
scope = "workspace:non_dev_node"

[checks.workspace-targets]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.workspace-unregistered]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.workspace-stale-entries]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.dev-only-boundary]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.unversioned-boundary]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.dead-workspace-packages]
tags = ["workspace"]
severity = "warn"
fast = false
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.subtree-remote-reachable]
tags = ["workspace"]
severity = "error"
fast = false
pure = false
needs_network = true
depends_on = []
scope = "workspace"

# workspace-unbuildable: a member whose manifest `uv sync` cannot resolve is
# also a member whose manifest the dependency scanners cannot read, and the CI
# router's paths filters are derived from what those scanners find. A manifest
# that stops parsing therefore removes dependency territories from the filters
# derived from it. The router derivation refuses outright on an unreadable
# manifest; this check is in "preflight" so the ordinary release path reports
# the broken manifest as its own finding instead of only as a router failure.
[checks.workspace-unbuildable]
tags = ["workspace", "preflight"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = []
scope = "workspace"

[checks.layers-violations]
tags = []
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace:non_dev_only"

# ---------------------------------------------------------------------------
# Dependency validation checks (from dep_validation.py)
# ---------------------------------------------------------------------------

[checks.deps-unused]
tags = []
severity = "error"
fast = false
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.deps-undeclared]
tags = []
severity = "error"
fast = false
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.deps-stale]
tags = []
severity = "error"
fast = false
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.deps-runtime-test-only]
tags = ["quality"]
severity = "warn"
fast = false
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.deps-dev-in-lib]
tags = ["quality"]
severity = "error"
fast = false
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.dead-modules]
tags = ["quality"]
severity = "warn"
fast = false
pure = true
needs_network = false
depends_on = []

[checks.dead-modules-stale]
tags = ["quality"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.circular-deps]
tags = ["quality"]
severity = "warn"
fast = false
pure = true
needs_network = false
depends_on = []

[checks.scaffold-unreplaced-vars]
tags = ["quality"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.ruff-lint]
tags = ["quality"]
severity = "error"
fast = true
pure = false
needs_network = false
depends_on = []

# ---------------------------------------------------------------------------
# Path-capable tool checks (rlsbl/tool_checks.py)
#
# Each runs a Python tool over a project-declared path list through the
# project's own environment (`uv run ...`), and each is paired with a pure
# competing-scope guard that hard-errors when the tool's own config file
# carries scope that would silently override or narrow those paths. The tool
# checks are impure (the tools write); the guards are pure config reads, so a
# preview runs them.
# ---------------------------------------------------------------------------

[checks.lint]
tags = ["quality", "preflight"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = []

[checks.lint-scope-guard]
tags = ["quality", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.format]
tags = ["quality", "preflight"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = []

[checks.format-scope-guard]
tags = ["quality", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.type-check]
tags = ["quality", "preflight"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = []

[checks.type-check-scope-guard]
tags = ["quality", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

# ---------------------------------------------------------------------------
# Phase 12 project checks
# ---------------------------------------------------------------------------

[checks.publish-mode-workflow]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.npm-private-mismatch]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.target-version-readable]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.dunder-version-missing]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.selfdoc-version-drift]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.scaffold-conflicts]
tags = ["project", "prepush", "release"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.cross-repo-path-sources]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

# target-matrix-fresh: the committed support matrix
# (rlsbl/data/support-matrix.json) is generated from the target, check and
# pipeline registries, and the docs directives render from it instead of
# importing rlsbl. A stale file therefore ships wrong documentation, so this
# regenerates in memory and compares. Skips visibly wherever the artifact does
# not exist -- it ships inside the rlsbl package, so only rlsbl's own tree has
# one. Pure (imports and serializes; no subprocess or network). In "preflight"
# so a stale matrix blocks the release that would publish it.
[checks.target-matrix-fresh]
tags = ["project", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

# requires-services: when config declares CI service containers (services /
# test_env), the rendered CI workflow(s) on disk must actually provision them.
# Pure filesystem read (config + workflow YAML), no subprocess/network.
[checks.requires-services]
tags = ["project"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["config-schema"]

# dev-sync overlay drift: reads the local dev-overlays sentinel and the venv's
# dist-info direct_url.json (filesystem only -- no subprocess, no network, so
# pure). In "preflight" so it blocks a LOCAL release when an overlay was wiped;
# skips cleanly on CI (the gitignored sentinel never exists there).
[checks.dev-overlay-drift]
tags = ["project", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

# ---------------------------------------------------------------------------
# Pre-push checks
# ---------------------------------------------------------------------------

[checks.prepush-changelog-coverage]
tags = ["prepush"]
severity = "error"
fast = false
pure = true
needs_network = false
depends_on = []

[checks.prepush-gitignore-guard]
tags = ["prepush"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.prepush-manual-warning]
tags = ["prepush"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []

[checks.test-suite]
tags = ["prepush", "quality", "preflight"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = ["prepush-changelog-coverage"]

[checks.test-suite-workspace]
tags = ["prepush", "workspace"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = ["prepush-changelog-coverage"]
scope = "workspace:non_dev_only"

# ---------------------------------------------------------------------------
# Maven-specific checks
# ---------------------------------------------------------------------------

[checks.maven-central-metadata]
tags = ["quality", "maven", "preflight"]
severity = "error"
fast = false
pure = false
needs_network = false
depends_on = []

# ---------------------------------------------------------------------------
# Scaffold checks
# ---------------------------------------------------------------------------

[checks.scaffold-gitignore-stale]
tags = ["workspace"]
severity = "warn"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.root-rlsbl-conflict]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.go-companion-tags]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.releasable-residue]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

[checks.member-pytest-config]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace:non_dev_node"

[checks.mixed-tag-schemes]
tags = ["workspace"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
scope = "workspace"

# ---------------------------------------------------------------------------
# Launcher pipeline checks
# ---------------------------------------------------------------------------

[checks.wrapper-producer]
tags = ["project", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["config-schema"]

# strictspec certificate deploy gate: consumes a configured strictspec diff
# certificate (format_version gate). Feature-flagged by config presence -- skips
# when the project has no strictspec_gate section. Pure (parses committed JSON/
# TOML, no subprocess or network). In "preflight" so it blocks a release.
[checks.strictspec-certificate-gate]
tags = ["project", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["config-schema"]

# stricttest floor adoption: the sandboxed test runner rlsbl distributes.
# Feature-flagged by adoption -- skips visibly when the repo declares neither a
# test_sandbox config section nor a stricttest dependency; hard-fails on a
# broken adopted state (runner missing or not executable, config family
# incomplete, a declared CI workflow that does not invoke the runner). Pure
# (reads committed JSON/TOML/YAML text, no subprocess or network). In
# "preflight" so a broken floor blocks a release.
[checks.stricttest-floor]
tags = ["project", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["config-schema"]

# Ecosystem-internal dependency floors: a release that requires new framework
# behavior must declare a ">=" floor at that version, or a consumer installing
# the published artifact resolves an older framework and breaks. Compares the
# declared constraint (pyproject.toml / package.json) against the locked
# version (uv.lock / package-lock.json); Go is structurally satisfied (require
# lines ARE the minimums). Feature-flagged by adoption -- skips visibly when
# the repo has no "internal_dep_floors" config key. Pure (reads committed
# TOML/JSON, no subprocess or network). In "preflight" so a lagging floor
# blocks the release that would ship it.
[checks.dep-floors]
tags = ["project", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = ["config-schema"]

# dep-locks: the question underneath dep-floors -- does each lockfile still
# resolve the manifest beside it? uv.lock and package-lock.json both record the
# requirements they resolved FROM, so a dependency added, removed or
# re-constrained after the lock was written is visible without resolving
# anything; go.sum is compared for coverage of go.mod's require lines. Pure and
# offline by construction: no package manager is invoked (see rlsbl/dep_locks.py
# for why `uv lock --check` was rejected). In "preflight" so a stale lock blocks
# the release whose bump would otherwise silently rewrite it.
[checks.dep-locks]
tags = ["project", "preflight"]
severity = "error"
fast = true
pure = true
needs_network = false
depends_on = []
