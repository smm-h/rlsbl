# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "Launcher artifact kind for wrapper-package publishing, handler migration to ctx-first signatures, and rename-releasable argument order fix."
# Optional context explaining why these changes were made
context = "Adds `artifact: \"launcher\"` pipeline kind that produces npm/PyPI shim packages downloading pre-built binaries from GitHub Releases. All 52 command handlers migrated to strictcli v0.29.0 ctx-first + Outcome signatures. The `monorepo rename-releasable` positional argument order now matches `mv` convention (`<old> <new>`)."
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["npm", "pypi"]
exclude = []
