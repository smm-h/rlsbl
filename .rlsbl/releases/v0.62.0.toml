# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Capability-gated publish/build_assets, Swift inheritance cleanup, version_file dir_path, and bug fixes."
# Optional context explaining why these changes were made
context = "Breaking: BaseTarget.build_assets() returns [] instead of raising NotImplementedError. publish() and build_assets() calls are now gated on target.capabilities. SwiftAppleTarget inherits from SwiftTarget. version_file() accepts optional dir_path for dynamic filename resolution. Plain exclusion list fixed. Flutter table cells corrected."
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "7e76a005798c7d6b81269bdefbd314df66c06fbf"

[tree_hashes]
"." = "a5b47c501fc59d2363895e8b7a65a089fccab8bd"
