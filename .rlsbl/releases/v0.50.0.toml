# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, or major
bump = "minor"
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "979dee000b7b2f37d485d0c00c1cfdee76082006"
description = "Config helpers and every target's `publish()` take the already-loaded config and context instead of a project root, removing redundant disk reads during publish."

[tree_hashes]
"." = "3d391c3ec94bdb3859c3cc5e4dc3c7ffa46d4bd1"
