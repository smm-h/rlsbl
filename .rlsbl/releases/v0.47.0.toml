# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, or major
bump = "minor"
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "19599be1c5a909eb1cd57971f8105f1e43043e01"
description = "`release init` and `release retry` scaffold empty fields that must be filled in deliberately, `watch` gains `--run-id`, the monorepo CI sync is rewritten around structured YAML with per-job `working-directory`, and `status` and `unreleased` scope their commits to the current project."

[tree_hashes]
"." = "e433b4b03868d2112ef012ea974369e3f2f40b20"
