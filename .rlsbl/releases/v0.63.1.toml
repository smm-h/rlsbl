# Version bump type: patch, minor, or major
bump = "patch"
# Short description of this release (required)
description = "Pass RLSBL_DIST_DIR env var to custom_assets build commands."
include = ["npm", "pypi"]
exclude = []
