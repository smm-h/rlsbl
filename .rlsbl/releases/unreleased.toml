# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "Multi-artifact releasables (per-member publishing at the shared version, collision-proof asset naming), version-consistency enforcement for published members, monorepo-init bootstrap fix"
# Optional context explaining why these changes were made
context = "Completes the six-todo scope of the Phase 10 multi-artifact releasable effort."
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["npm", "pypi"]
exclude = []
