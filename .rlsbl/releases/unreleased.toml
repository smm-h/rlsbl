# Version bump type: patch, minor, or major
bump = "patch"
# Short description of this release (required)
description = "Release undo traceback logging, per-version .md canonical regeneration, test isolation fixes, and CI JUnit XML reporting."
# Optional context explaining why these changes were made
context = ""
include = ["npm", "pypi"]
exclude = []
